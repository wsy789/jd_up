<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <com.youth.banner.Banner
        android:id="@+id/list_recomment_banner"
        android:layout_width="match_parent"
        android:layout_height="230dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"/>
        <TextView
            android:id="@+id/banner_recomment_tv"
            android:layout_width="match_parent"
            android:layout_height="23dp"
            android:textColor="@color/back"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintBottom_toBottomOf="@+id/list_recomment_banner"
            android:background="@color/shallow_subtitle"
            />


    <!--    <ImageView
            android:id="@+id/item1_img"
            android:layout_width="match_parent"
            android:layout_height="220dp"
            app:layout_constraintTop_toTopOf="parent"/>

        <TextView
            android:id="@+id/item1_tv"
            app:layout_constraintBottom_toBottomOf="@+id/item1_img"
            android:layout_width="match_parent"
            android:layout_height="30dp"
            android:textSize="20sp"
            android:background="@color/colortitle"/>-->
</androidx.constraintlayout.widget.ConstraintLayout>